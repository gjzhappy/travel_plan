const $=s=>document.querySelector(s), colors=['#d66a3b','#1e6a5a','#8b7355','#527b91'];
let plan, planId, events=[], review={}, explainability={}, activeDay=1, versionHistory=[];
document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{const t=$('#request');if(!t.value.includes(b.dataset.add))t.value=t.value.replace(/[。 ]*$/,'，')+b.dataset.add+'。';t.focus()});
loadDemos();
loadSystemStatus();
async function loadSystemStatus(){try{const s=await api('/api/system/status'),agent=s.agent_runtime==='opencode'?'OpenCode Agent':'Deterministic Offline Agent';$('#runtime-status').textContent=`运行环境：离线演示模式 · Agent：${agent} · 数据：上海旅游知识库`}catch(e){$('#runtime-status').textContent='运行环境：离线演示模式 · 数据：上海旅游知识库'}}
async function loadDemos(){try{const demos=await api('/api/demo');$('#demo-list').innerHTML=demos.map(d=>`<article class="demo-card"><div><b>${safe(d.name)}</b><p>${safe(d.description)}</p></div><button type="button" data-demo="${safe(d.id)}">生成方案 <span>→</span></button></article>`).join('')||'<span class="demo-placeholder">暂无演示场景</span>';document.querySelectorAll('[data-demo]').forEach(b=>b.onclick=()=>runDemo(b.dataset.demo,b))}catch(e){$('#demo-list').innerHTML='<span class="demo-placeholder">演示场景暂不可用，也可以在下方输入需求。</span>'}}
async function runDemo(id,button){button.disabled=true;show('loading');$('#loading-copy').textContent='载入固定场景并理解旅行需求…';try{const data=await api(`/api/demo/${encodeURIComponent(id)}/run`,{method:'POST'});await loadPlan(data);activeDay=1;render();show('result')}catch(err){$('#error span').textContent=err.message;show('error')}finally{button.disabled=false}}
$('#planner-form').onsubmit=async e=>{e.preventDefault();show('loading');let steps=['核对开放时间与地点距离…','组合每日景点与用餐地点…','完成行程体验审核…'],i=0;const timer=setInterval(()=>$('#loading-copy').textContent=steps[++i%steps.length],900);try{const data=await api('/api/plans',{method:'POST',body:JSON.stringify({request:$('#request').value})});await loadPlan(data);activeDay=1;render();show('result')}catch(err){$('#error span').textContent=err.message;show('error')}finally{clearInterval(timer)}};
$('#restart').onclick=$('#error button').onclick=()=>show('hero');
$('#modify-form').onsubmit=async e=>{e.preventDefault();const button=e.submitter;button.disabled=true;button.firstChild.textContent='正在重新规划 ';try{const data=await api(`/api/plans/${planId}/modify`,{method:'POST',body:JSON.stringify({scope:$('#modify-scope').value,target:$('#modify-target').value||null,instruction:$('#modify-instruction').value})});await loadPlan(data);activeDay=Math.min(activeDay,plan.days.length);render();$('#modify-instruction').value='';setTimeout(()=>$('#change-story').scrollIntoView({behavior:'smooth',block:'center'}),100)}catch(err){alert(err.message)}finally{button.disabled=false;button.firstChild.textContent='重新规划 '}};
async function api(url,options={}){const response=await fetch(url,{headers:{'Content-Type':'application/json'},...options});const data=await response.json();if(!response.ok)throw new Error(data.error||'请求失败');return data}
async function loadPlan(data){planId=data.plan_id;plan=data.plan;[events,review,explainability]=await Promise.all([api(`/api/plans/${planId}/events`),api(`/api/plans/${planId}/review`),api(`/api/plans/${planId}/explainability`)])}
function show(id){['hero','loading','error','result'].forEach(x=>$('#'+x).classList.toggle('hidden',x!==id));scrollTo({top:0,behavior:'smooth'})}
async function renderVersions(){versionHistory=await api(`/api/plans/${planId}/versions`);$('#versions').innerHTML='<span>版本变化</span>'+versionHistory.map(v=>`<button class="${v.version===plan.version?'active':''}" data-version="${v.version}">v${v.version}</button>`).join('');document.querySelectorAll('[data-version]').forEach(b=>b.onclick=()=>{const snapshot=versionHistory.find(v=>v.version===+b.dataset.version);plan=snapshot.plan;events=snapshot.events;review=snapshot.review;explainability=snapshot.explainability;activeDay=Math.min(activeDay,plan.days.length);render()});const current=versionHistory.find(v=>v.version===plan.version),story=$('#change-story');story.classList.toggle('hidden',!current?.change);story.innerHTML=current?.change?`<span>原计划 v${current.version-1}</span><i>→</i><span><small>你的调整</small>${safe(current.change.instruction)}</span><i>→</i><b>重新规划 v${current.version}</b>`:''}
function render(){const count=plan.days.reduce((n,d)=>n+d.summary.attraction_count,0);$('#trip-meta').textContent=`${plan.days.length} 天 · ${count} 处景点`;renderOverview();$('#day-tabs').innerHTML=plan.days.map(d=>`<button class="${d.day===activeDay?'active':''}" data-day="${d.day}">DAY ${d.day}</button>`).join('');document.querySelectorAll('[data-day]').forEach(b=>b.onclick=()=>{activeDay=+b.dataset.day;render()});renderDay();renderMap();renderHotels();renderBudget();renderVersions();renderExplainability();renderProcess();$('#events').innerHTML=events.length?events.map((x,i)=>`<div><b>${safe(x.message)}</b>${i<events.length-1?'<i>↓</i>':''}</div>`).join(''):'<p>当前版本暂无过程记录</p>';$('#review').innerHTML=`<div class="review-summary ${review.passed?'pass':'warn'}"><b>${review.passed?'✓ 体验审核通过':'! 有体验建议'}</b><span>${safe(review.summary)}</span></div>${(review.checks||[]).map(x=>`<div class="review-check"><span>${safe(x.status)}</span><b>${safe(x.type)}</b><p>${safe(x.message)}</p></div>`).join('')}`}
function renderOverview(){const o=plan.overview||{},facts=[['👨‍👩‍👧','人员',o.party],['🚇','出行',o.transport],['🏨','住宿',o.lodging],['💰','预算',o.budget==null?null:`¥${money(o.budget)}`]].filter(x=>x[2]);$('#overview-title').textContent=o.title||'我的旅行方案';$('#overview-facts').innerHTML=facts.map(x=>`<div><i>${x[0]}</i><span>${x[1]}</span><b>${safe(x[2])}</b></div>`).join('');$('#route-feature').textContent=(o.route_features||[]).join(' → ')||'以每日主题串联行程'}
function renderProcess(){const stages=new Set(events.map(x=>x.stage)),steps=[];if(stages.has('REQUIREMENT'))steps.push('理解你的需求');if(stages.has('ROUTE_PLAN'))steps.push('找到适合你的景点','优化每日路线','安排餐饮住宿');if(stages.has('VALIDATE'))steps.push('检查是否可行');if(stages.has('REVIEW'))steps.push('完成旅行方案');$('#process-steps').innerHTML=steps.map(x=>`<span>✓ ${safe(x)}</span>`).join('')}
function renderExplainability(){const x=explainability||{},v=x.validator||{};$('#explain-notice').textContent=x.notice||'当前快照暂无解释数据';$('#explain-sources').innerHTML=(x.sources||[]).map(s=>`<span class="${s.available?'available':'missing'}"><i></i>${safe(s.label)}</span>`).join('');$('#explanation').innerHTML=(x.days||[]).map(d=>`<article><h4>${safe(d.title)} <em>${safe(d.source)}</em></h4><div class="reason-grid">${(d.reasons||[]).map(r=>`<section class="reason ${r.available?'':'unavailable'}"><b>${safe(r.category)}</b>${r.details.map(detail=>`<p>${r.available?'✓':'—'} ${safe(detail)}</p>`).join('')}<small>${safe(r.source)}</small></section>`).join('')}</div></article>`).join('')||'<p>当前版本暂无每日依据</p>';$('#validator').innerHTML=`<div class="validation-state ${v.status==='PASSED'?'pass':'warn'}"><b>${v.status==='PASSED'?'✓ 最终门禁通过':v.status==='BLOCKED'?'! 最终门禁阻止':'— 未记录校验'}</b><span>${safe(v.stage||'无阶段信息')}</span></div>${(v.issues||[]).map(i=>`<p>${safe(i.code||i.type||'ISSUE')} · ${safe(i.message||'')}</p>`).join('')}`}
function renderDay(){const d=plan.days.find(x=>x.day===activeDay)||plan.days[0],timeline=d.timeline||d.nodes;$('#day-panel').innerHTML=`<div class="day-intro"><div><span class="section-kicker">DAY ${d.day}</span><h3>${safe(d.theme)}</h3></div><p>${d.date}<br>${d.summary.attraction_count} 处景点 · 路上约 ${d.summary.travel_minutes} 分钟</p></div><div class="timeline">${timeline.map(n=>`<div class="node ${['lunch','dinner'].includes(n.type)?'meal':n.type.startsWith('hotel_')||n.type==='luggage_drop'?'hotel':''}"><div class="time">${safe(n.start_time)}${n.end_time?`<small>–${safe(n.end_time)}</small>`:''}</div><div class="rail"><div class="dot"></div></div><div class="node-content"><h4>${safe(n.name)} <span class="tag">${safe(n.display.type_label)}</span></h4><p>${safe(n.display.detail)}</p>${n.duration_min?`<div class="travel">${mode(n.transport_mode)} · ${Number(n.distance_km||0).toFixed(1)} km · ${n.duration_min} 分钟</div>`:''}</div></div>`).join('')}</div>`}
function renderMap(){let selected=plan.days.find(d=>d.day===activeDay)||plan.days[0],route=selected.route_visualization||{nodes:[],edges:[]},nodes=route.nodes;$('#map-caption').textContent=`第 ${selected.day} 天 · ${nodes.length} 站`;if(!nodes.length){$('#route-map').innerHTML='';$('#map-key').innerHTML='<span>暂无位置数据</span>';return}let lats=nodes.map(n=>n.lat),lons=nodes.map(n=>n.lng),minLat=Math.min(...lats)-.015,maxLat=Math.max(...lats)+.015,minLon=Math.min(...lons)-.02,maxLon=Math.max(...lons)+.02;const pt=n=>[45+(n.lng-minLon)/(maxLon-minLon||1)*430,520-(n.lat-minLat)/(maxLat-minLat||1)*470],color=colors[(selected.day-1)%colors.length],byOrder=Object.fromEntries(nodes.map(n=>[n.order,n]));$('#route-map').innerHTML=`<defs><marker id="route-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="${color}"/></marker></defs>`+route.edges.map(e=>{const a=pt(byOrder[e.from]),b=pt(byOrder[e.to]);return `<line class="route-line" stroke="${color}" marker-end="url(#route-arrow)" x1="${a[0]}" y1="${a[1]}" x2="${b[0]}" y2="${b[1]}"/>`}).join('')+nodes.map(n=>{let [x,y]=pt(n);return `<circle class="route-dot" fill="${color}" cx="${x}" cy="${y}" r="12"/><text class="route-order" x="${x}" y="${y+4}">${n.order}</text><text class="route-label" x="${x+17}" y="${y+4}">${safe(n.name)}</text>`}).join('');$('#map-key').innerHTML=nodes.map(n=>`<span><i style="background:${color}"></i>${n.order} ${safe(n.name)}</span>`).join('')}
function renderHotels(){$('#hotels').innerHTML=plan.hotels.map(h=>`<div class="hotel-row"><b>${safe(h.name)}</b><span>第 ${h.start_day}–${h.end_day} 天 · ¥${money(h.nightly_price)}/晚</span></div>`).join('')}
function renderBudget(){let b=plan.budget,items=[['门票',b.tickets],['餐饮',b.meals],['住宿',b.hotels],['市内交通',b.transport],['预计总计',b.total]];$('#budget').innerHTML=items.map((x,i)=>`<div class="${i===4?'total':''}"><span>${x[0]}</span><b>¥${money(x[1])}</b></div>`).join('')}
function money(n){return Number(n).toLocaleString('zh-CN',{maximumFractionDigits:0})}function mode(x){return({public_transit:'公共交通',walking:'步行',taxi:'出租车'}[x]||x||'交通')}function safe(x){const e=document.createElement('span');e.textContent=String(x??'');return e.innerHTML}
