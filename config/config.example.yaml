qdrant_top_k: 24
agent_runtime_mode: deterministic
data_mode: offline
external_provider_mode: mock
daily_start_time: "08:30"
daily_latest_end_time: "21:30"
lunch_window: ["11:30", "13:30"]
dinner_window: ["17:30", "20:00"]
# Computational guard; daily selection stops on time/route feasibility first.
max_pois_per_day: 6
hotel_change_min_gain: 60
review_max_retries: 2
transport_mode: public_transit
mock_mode: true
mock_reference_date: "2026-01-01"
mock_retrieved_at: "2026-01-01T00:00:00Z"
state_dir: data/state
# Daily transport policy tuples are: preferred total, hard total,
# preferred single leg, hard single leg (minutes).
transport_relaxed: [150, 240, 60, 90]
transport_moderate: [180, 270, 70, 100]
transport_intensive: [210, 300, 80, 110]
transport_long_transfer_min: 60
